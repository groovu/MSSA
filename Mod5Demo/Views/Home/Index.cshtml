@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@*@inject Mod5Demo.Models.IFormat format*@
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width"/>
    <title>Index</title>
</head>
<body>
    <div>
        Concatenated text: @String.Concat("hi", "all");
        @for (int i = 0; i < 5; i ++)
            {
                <span>@i</span>
            }
    </div>
    <br/>
    @foreach(string name in ViewBag.names)
            {
                <span>@name</span>
            }
    <span> Price with tax: @ViewBag.Price * 1.2</span>
    <span> Price with tax: @(ViewBag.Price * 1.2)</span>

    <br/>
    @*<div>@format.GetFormattedNumber(434343)</div>*@
    @Html.ActionLink("Go To about page", "About")
       
</body>
</html>